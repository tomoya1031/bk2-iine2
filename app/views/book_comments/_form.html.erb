<%= form_with(model: [book, book_comment] ) do |f| %>
    <div class="comment-text">
        <%= f.text_area :comment, class: "comment", remote: true  %>
    </div>
    <div class="comment-text">
        <%= f.submit "送信", class: "btn btn-base-1 comment-sbmit" %>
    </div>
<% end %>
